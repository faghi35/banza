# Banza AI Frontend — PRODUCTION (Vercel) — modèle
# Copier vers .env.production ou (recommandé) définir dans le
# dashboard Vercel SANS valeur :
#   NEXT_PUBLIC_API_URL = (vide)
#
# Le proxy backend est contrôlé côté serveur par next.config.mjs :
#   BACKEND_ORIGIN = http://banza-ai.hopetrade-rdc.com  (défaut prod)
NEXT_PUBLIC_API_URL=