# Banza AI Frontend — PRODUCTION (Vercel / Serveur de production)
# ============================================================
# L'API est appelée en URLs RELATIVES (/api/...) et relayée par le proxy / rewrite
# vers le backend PHP.
VITE_API_URL=
BACKEND_ORIGIN=https://banza-ai.onekana-agency.com